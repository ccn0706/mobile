import {
  get,
  post
} from './http'

// 用法

// // 导出
// export const getInfo = (params) => get('/api/v1/jx/getInfo', params)
// // 导入
// import {
//   getInfo
// } from 'api/home'
// getInfo().then((res) => {
//   if (res.code === 200) {}
// }).catch((err) => {})
